{
  "name": "tenant_created",
  "version": 1,
  "description": "A new tenant was created",
  "schema": {
    "type": "object",
    "required": ["event_id", "occurred_at", "tenant_id"],
    "properties": {
      "event_id": {"type": "string"},
      "occurred_at": {"type": "string"},
      "tenant_id": {"type": "integer"},
      "tenant_name": {"type": ["string", "null"]}
    }
  }
}
