if ($request_method !~ ^(GET|POST|PUT|PATCH|DELETE|OPTIONS|HEAD)$) {
    return 405;
}

if ($request_uri ~* "(?:\.\./|<script|union(?:\s+all)?\s+select|/etc/passwd|wp-admin|phpmyadmin)") {
    return 403;
}
